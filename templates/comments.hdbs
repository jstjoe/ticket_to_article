<ul class="comment_list">
  <h5>Comments: </h5><br>
  {{#each comments}}
    {{#if public}}
      <li class="comment" id="{{id}}">{{created_at_local}} <span class="comment">{{{html_body}}}</span></li>
    {{/if}}
  {{/each}}
</ul>